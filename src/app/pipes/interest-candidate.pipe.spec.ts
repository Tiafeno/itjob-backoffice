import { InterestCandidatePipe } from './interest-candidate.pipe';

describe('InterestCandidatePipe', () => {
  it('create an instance', () => {
    const pipe = new InterestCandidatePipe();
    expect(pipe).toBeTruthy();
  });
});
